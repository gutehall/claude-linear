Use the Linear MCP tool to:
1. Fetch all projects and display a numbered list showing only projects with status Backlog, Planned, or In Progress. Include columns: number, name, status.
2. Ask the user to pick a project by number
3. Fetch and display all issues in that project with state "Ready for build" in a concise table: issue ID, priority, title, assignee (if any)
